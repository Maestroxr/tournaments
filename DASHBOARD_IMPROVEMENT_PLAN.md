# תוכנית שיפור הדרגתית — דשבורד מנהל הטורנירים

מטרת התוכנית היא להפוך את הדשבורד למסך עבודה מהיר וברור שבו המארגן מבין מיד:

1. מה קורה עכשיו.
2. מה דורש טיפול.
3. מה הפעולה הבאה.
4. מה מצב ההכנסות, ההוצאות והנטו של הטורנירים.

התוכנית משלימה את `ORGANIZER_ROADMAP.md` ומתמקדת בדשבורד הראשי ובמעבר ממנו לחדר הבקרה של טורניר פעיל.

## שיטת העבודה

- מבצעים שלב קטן אחד בכל פעם.
- בכל שלב משנים נושא מרכזי אחד בלבד.
- בסוף כל שלב מריצים בדיקות, מציגים את התוצאה ומחליטים יחד אם לאשר, לדייק או לבטל.
- לא עוברים לשלב הבא לפני שהשלב הנוכחי מאושר.
- שומרים על ה-API והתנהגות קיימת ככל האפשר; שינוי backend נעשה רק כשהמסך באמת צריך נתון חדש.
- כל שלב צריך להשאיר את המערכת במצב עובד וניתן לחזרה.

## סדר השלבים

### שלב 0 — צילום מצב וקביעת קו בסיס

**סטטוס:** הושלם ב-7 בספטמבר 2026. התוצאות שמורות ב-`.codex-run/dashboard-baseline/BASELINE_REPORT.md`.

**שינוי:** אין שינוי מוצרי.

- מפעילים את ה-frontend וה-backend המקומיים.
- מצלמים את הדשבורד בדסקטופ ובמובייל, בעברית ובאנגלית.
- רושמים שגיאות קיימות בקונסול, בבדיקות ובתצוגה.
- בוחרים נתוני דמו קבועים שמכסים: טורניר פעיל, טורניר קרוב, חסימה ומשתמש חדש.

**סיום השלב:** יש ארבע תמונות בסיס ורשימת תקלות קיימות, כדי שנוכל להשוות כל שינוי למצב המקורי.

**נקודת החלטה:** מאשרים שהמסך שנבדק הוא אכן הדשבורד שצריך לשפר.

---

### שלב 1 — השלמת עברית ו-RTL בדשבורד

**סטטוס:** אושר ב-7 בספטמבר 2026. הפירוט שמור ב-`.codex-run/dashboard-stage-1/STAGE_1_REPORT.md`.

**שינוי:** טקסטים בלבד.

- מעבירים את כל הטקסטים הקשיחים ב-`DashboardView.vue` למערכת התרגום הקיימת.
- מתרגמים כותרות, KPI, כפתורים, מצבי ריק, שגיאות והודעות זמן.
- מתקנים אייקונים, חצים וריווחים שאינם מתהפכים נכון ב-RTL.
- משאירים מספרים, שעות ומזהי משחק בכיוון קריא.

**סיום השלב:** אין טקסט אנגלי בדשבורד במצב עברית, ואין שבירת פריסה ב-RTL.

**בדיקות:** בדיקת רכיב לתרגום אנגלית/עברית ובדיקה חזותית בשני הכיוונים.

**נקודת החלטה:** מאשרים את הניסוחים בעברית ואת סדר האלמנטים.

---

### שלב 2 — סידור החלק העליון של המסך

**סטטוס:** אושר ב-7 בספטמבר 2026. הפירוט שמור ב-`.codex-run/dashboard-stage-2/STAGE_2_REPORT.md`.

**שינוי:** היררכיה בלבד, ללא נתונים חדשים.

- מצמצמים את כותרת הדף ואת אזור יצירת הטורניר.
- מציגים פעולה ראשית אחת: `יצירת טורניר` כאשר אין פעילות מתאימה, או `המשך ניהול` כאשר יש טורניר פעיל.
- מעבירים את זמן העדכון והרענון לאזור משני ושקט יותר.
- מוודאים שהמידע החשוב מתחיל בתוך המסך הראשון ללא גלילה.

**סיום השלב:** במסך הראשון רואים פעולה ראשית, מדדי מצב ותחילת אזור הטיפול.

**בדיקות:** דסקטופ 1440px, לפטופ 1024px ומובייל 390px.

**נקודת החלטה:** מאשרים מהי הפעולה הראשית ובאיזה מצב היא משתנה.

---

### שלב 3 — כרטיס “האירוע הבא”

**סטטוס:** אושר ב-7 בספטמבר 2026. השינוי הוגבל ל-`DashboardView.vue` ולבדיקות הדשבורד; הפירוט שמור ב-`.codex-run/dashboard-stage-3/STAGE_3_REPORT.md`.

**שינוי:** רכיב אחד חדש המבוסס תחילה על הנתונים הקיימים.

- מציגים את הטורניר הפעיל בעדיפות ראשונה; אם אין פעיל, מציגים את הטורניר הקרוב ביותר.
- מציגים שם, סטטוס, תאריך/ספירה לאחור, מספר משתתפים והפעולה הבאה.
- מוסיפים CTA יחיד: `פתח חדר בקרה`, `המשך הכנה` או `נהל הרשמה`.
- מוסיפים מצב ריק ברור אם עדיין אין טורניר.

**סיום השלב:** המארגן יכול להיכנס לעבודה המרכזית שלו בלחיצה אחת.

**בדיקות:** פעיל, קרוב, טיוטה וללא טורנירים.

**נקודת החלטה:** מאשרים איזה טורניר מקבל עדיפות ומה מוצג בכרטיס.

---

### שלב 4 — ארגון היררכיית הדשבורד והסרת כפילויות

**סטטוס:** מוכן לאישור. השינוי הוגבל לדשבורד, לתרגומים ולבדיקות שלו, ללא שינוי API.

**שינוי:** סידור התוכן הקיים לפי חשיבות תפעולית.

- משאירים למעלה רצועה קומפקטית של הפעולה הבאה.
- מעבירים את `דורש טיפול` מיד לאחר הפעולה הבאה.
- מרכזים את המידע העתידי באזור `היום ובקרוב` ומצמידים אליו את מסנן התקופה.
- מעבירים את ארבעת המדדים התפעוליים למיקום משני.
- לא מציגים שוב ככרטיס את הטורניר הפעיל שכבר מופיע בפעולה הבאה.
- מסירים את אזור `משתמשים חדשים` מהדשבורד הראשי.

**סיום השלב:** סדר המסך הוא פעולה הבאה, דורש טיפול, היום ובקרוב, מדדי מצב ורק לאחר מכן טורנירים פעילים נוספים.

**בדיקות:** טורניר פעיל יחיד, כמה טורנירים פעילים, טורניר קרוב ללא פעיל, טיוטה, מצב ריק, עברית ו-RTL.

**נקודת החלטה:** מאשרים את סדר האזורים ואת הסרת הכפילויות לפני העשרת `דורש טיפול`.

---

### שלב 5 — “דורש טיפול” כתור עבודה

**סטטוס:** מוכן לאישור. השינוי הוגבל לדשבורד, לתרגומים ולבדיקות שלו, ללא שינוי API.

**שינוי:** שיפור הרשימה הקיימת לפני הוספת סוגי התראות חדשים.

- מעבירים את הרשימה מעל תוכן משני.
- ממיינים לפי חומרה ולאחר מכן לפי המועד הידוע של הטורניר.
- מציגים סיבה קצרה, זמן/דחיפות ופעולה ישירה לפתרון.
- מאפשרים סינון מהיר: הכול, קריטי, הרשמה ותוצאות.
- מציגים מצב תקין קומפקטי כאשר הרשימה ריקה.

**סיום השלב:** כל פריט מסביר מה הבעיה, כמה זמן היא קיימת ומה לעשות עכשיו.

**בדיקות:** רשימה ריקה, פריט יחיד, יותר משמונה פריטים וכל סוג חומרה.

**נקודת החלטה:** מאשרים את סוגי ההתראות והניסוח שלהן.

---

### שלב 6 — כרטיסי טורנירים פעילים

**סטטוס:** מוכן לאישור. נוסף מידע תפעולי ל־API של הדשבורד ולכרטיסים הפעילים, ללא שינוי במסכי הטורניר האחרים.

**שינוי:** שיפור כרטיס קיים אחד.

- מוסיפים פס התקדמות של הסיבוב או הטורניר.
- מציגים משחקים שהושלמו בסיבוב, משחקים ממתינים והמשחק הבא.
- לא מציגים נתון `משחקים חיים` עד שהמערכת תשמור סטטוס חי אמין.
- מחליפים מידע משני במידע שעוזר לקבל החלטה.
- שומרים על פעולה ראשית אחת בכל כרטיס.

**סיום השלב:** ניתן להשוות במהירות בין כמה טורנירים ולהבין איזה מהם דורש כניסה מיידית.

**בדיקות:** טורניר בתחילת הדרך, באמצע, לקראת סיום ועם חריגה.

**נקודת החלטה:** מאשרים אילו שלושה נתונים נשארים גלויים תמיד.

---

### שלב 7 — נתוני הרשמה במקום “משתמשים חדשים”

**סטטוס:** מוכן לאישור. נתוני ההרשמה נוספו ל־API של הדשבורד ולאזור `היום ובקרוב`.

**שינוי:** החלפת אזור משני אחד.

- מציגים התקדמות הרשמה מול מינימום השחקנים.
- מציגים תשלומים חסרים, צ'ק-אין ורשימת המתנה מתוך מודל ההרשמה הקיים.
- מציגים תשלום תקין כמצב חיובי ותשלום חסר כאזהרה.
- משאירים פעולה ישירה לניהול המשתתפים בכל טורניר.

**סיום השלב:** האזור התחתון עוזר להכין טורניר ולא רק מתאר פעילות כללית במערכת.

**בדיקות:** ללא הרשמות, הרשמה חלקית, קיבולת מלאה ותשלום חסר.

**נקודת החלטה:** מאשרים את ארבעת נתוני המוכנות ואת מיקומם בתוך `היום ובקרוב`.

---

### שלב 8 — Activity Feed בסיסי

**סטטוס:** מוכן לאישור. נוסף Feed של חמש הפעולות האחרונות מתוך נתוני Audit ותנועות קיימים.

**שינוי:** רכיב קטן ו-endpoint ממוקד, אם נתוני Audit קיימים.

- מציגים פעולות אחרונות: הרשמה, שינוי תוצאה ותנועת ארנק.
- מציגים מי ביצע, מה השתנה ומתי כאשר המידע נשמר במקור.
- לא מציגים שינוי מצב טורניר עד שקיים עבורו Audit אמין.
- מתחילים בחמש פעולות אחרונות ללא חיפוש מתקדם.
- לא משכפלים מידע שכבר מופיע באזור “דורש טיפול”.

**סיום השלב:** המארגן יכול להבין מה השתנה מאז הפעם האחרונה שנכנס.

**בדיקות:** הרשאות, סדר כרונולוגי, ערכים חסרים וקישורים לישות הרלוונטית.

**נקודת החלטה:** מאשרים אילו פעולות ראויות להופיע בדשבורד הראשי.

---

### שלב 9 — הפרדת “דשבורד כללי” מ“חדר בקרה חי”

**סטטוס:** מוכן לאישור. השינוי הוגבל ליעדי הפעולות בדשבורד וב־API שלו; חדר הבקרה עצמו לא שונה.

**שינוי:** ניווט והגדרת אחריות למסכים; ללא בנייה מחדש מלאה של ה-Live.

- הדשבורד הכללי מנהל מספר טורנירים והכנה.
- חדר הבקרה מטפל בטורניר פעיל אחד בלבד.
- מוסיפים מעבר ברור מהכרטיס הראשי לחדר הבקרה.
- משתמשים בנתיבים הישירים `live`, `players` ו־`overview` במקום נתיבי redirect ישנים.
- מונעים הצגת נתוני Live מפורטים פעמיים בשני המסכים.

**סיום השלב:** לכל מסך יש מטרה אחת ברורה ואין כפילות תפעולית.

**בדיקות:** ניווט ישיר, חזרה לדשבורד, הרשאות וטורניר שאינו פעיל.

**נקודת החלטה:** מאשרים את הגבול בין שני המסכים לפני שיפור חדר הבקרה עצמו.

---

### שלב 10 — הגדרת הנתונים והחישובים הפיננסיים

**סטטוס:** מוכן לאישור. נוסף API ניהולי ב־`/api/admin/finance` עם טווחים של 7, 30, 90 ימים וכל התקופה. הפקדות ומשיכות אינן נכללות, תנועות עם סימן שגוי מסוננות, והתשלומים הפתוחים מוצגים כתמונת מצב נוכחית.

**שינוי:** endpoint פיננסי ממוקד המבוסס על הנתונים שכבר קיימים במערכת, ללא גרפים.

- מגדירים `הכנסות` כדמי הרשמה ששולמו בפועל, ולא כמספר המשתתפים כפול דמי הכניסה.
- מגדירים `החזרים` ו`פרסים` כהוצאות טורניר נפרדות.
- מחשבים `נטו מטורנירים = הכנסות - החזרים - פרסים`.
- מציגים בנפרד תשלומים שטרם נגבו.
- מפרידים הפקדות ומשיכות ארנק מהכנסות והוצאות עסקיות.
- תומכים בטווחי זמן: 7 ימים, 30 ימים, 90 ימים והכול.

**סיום השלב:** קיימת תשובה חד-משמעית ומתועדת לכל נתון פיננסי, וה-API מחזיר סכומים עקביים ללא שינוי חזותי בדשבורד.

**בדיקות:** טורניר חינמי, תשלום מלא, תשלום חסר, ויתור, החזר, פרס וטורניר ללא תנועות.

**נקודת החלטה:** מאשרים את הגדרות ההכנסה, ההוצאה והנטו לפני הצגתן למשתמש.

---

### שלב 11 — סיכום פיננסי בדשבורד

**סטטוס:** מוכן לאישור. נוסף לדשבורד המנהלים תקציר קבוע ל־30 הימים האחרונים וקישור לדף הפיננסי המלא תחת אזור ההעברות.

**שינוי:** הוספת אזור פיננסי קומפקטי ללא גרפים.

- מציגים ארבעה סכומים: הכנסות, הוצאות מתועדות, נטו מטורנירים ותשלומים פתוחים.
- מפרטים החזרים ופרסים מתחת לסכום ההוצאות.
- שומרים על תקופה קבועה של 30 ימים בדשבורד; מסנן התקופה נמצא בדף המלא כדי לא להעמיס על הדשבורד.
- ממקמים את האזור מתחת לחלקים התפעוליים `דורש טיפול` ו`היום ובקרוב`.
- משתמשים בצבע כדי להבדיל בין הכנסה, הוצאה ונטו, אך לא מסתמכים על צבע בלבד.

**סיום השלב:** אפשר להבין את התמונה הפיננסית הבסיסית בלי שהמסך התפעולי יאבד את העדיפות שלו.

**בדיקות:** כל הסכומים אפס, נטו חיובי, נטו שלילי, סכומים גדולים, RTL ומובייל.

**נקודת החלטה:** מאשרים את ארבעת הסכומים, המיקום והניסוחים לפני הוספת גרפים.

---

### שלב 12 — גרפים פיננסיים ממוקדים

**סטטוס:** מוכן לאישור. נוסף דף `Financial overview` ב־`/transfers/finance`, בתוך אזור העברות עם תפריט צד. הדף משתמש ברכיבי PrimeVue הקיימים ובגרף SVG נגיש, ללא תלות חדשה.

**שינוי:** שתי תצוגות נתונים בלבד, המבוססות על הנתונים שאושרו בשלב הקודם.

- מוסיפים גרף מגמה של הכנסות מול הוצאות לאורך זמן.
- מוסיפים טבלת השוואה בין טורנירים עם פסי נטו: הכנסה, הוצאה ונטו.
- מוסיפים tooltip, מקרא וערכים נגישים לקורא מסך.
- מציגים מצב ריק מועיל כאשר אין די נתונים לגרף.
- נמנעים מגרפים דקורטיביים או מכפילויות של אותם סכומים.

**סיום השלב:** התצוגות עונות על שתי שאלות בלבד: כיצד המצב משתנה לאורך זמן, ואילו טורנירים מציגים נטו גבוה יותר.

**בדיקות:** נקודת נתון אחת, תקופה ללא פעילות, ערכים שליליים, טורנירים רבים, מובייל ו-RTL.

**נקודת החלטה:** מאשרים את סוגי הגרפים ורמת הפירוט שלהם.

---

### שלב 13 — הוצאות חיצוניות ורווח מלא

**שינוי:** מודל חדש להזנת הוצאות שאינן קיימות כיום בתנועות הארנק.

- מוסיפים הוצאה עם סכום, קטגוריה, תאריך, הערה וטורניר אופציונלי.
- מתחילים בקטגוריות: מקום, ציוד, צוות, שיווק והוצאה אחרת.
- מוסיפים יצירה, עריכה וביטול עם Audit והרשאות מתאימות.
- מעדכנים את החישוב ל`רווח = הכנסות - החזרים - פרסים - הוצאות חיצוניות`.
- רק לאחר שהמודל פעיל מחליפים בממשק את המונח `נטו מטורנירים` ב`רווח`.

**סיום השלב:** הדשבורד מציג רווח עסקי המבוסס על כל ההוצאות הידועות, ולא הערכה חלקית.

**בדיקות:** הוצאה כללית, הוצאה המשויכת לטורניר, עריכה, ביטול, הרשאות וחישוב מחדש.

**נקודת החלטה:** מאשרים את הקטגוריות ואת תהליך הזנת ההוצאות לפני הרחבה לדוחות פיננסיים.

---

### שלב 14 — שפה חזותית ומערכת צבעים

**שינוי:** CSS וטוקנים בלבד ככל האפשר.

- מאחדים את הגדרות Light/Dark ומסירים דריסות כפולות.
- מגדירים צבעי רקע, משטח, גבול, טקסט, הצלחה, אזהרה וקריטי כמשתנים.
- קובעים רדיוס, צל, ריווח וגובה רכיבים עקביים.
- מוסיפים זהות שש-בש עדינה בכותרת או באיור יחיד, ללא עומס.
- שוקלים מצב בהיר להכנה ומצב כהה לחדר הבקרה החי.

**סיום השלב:** אין צבעים סותרים או `!important` חדשים, וכל אזורי הדשבורד משתמשים באותה שפה חזותית.

**בדיקות:** ניגודיות, Light/Dark, עברית/אנגלית ומצבי hover/focus.

**נקודת החלטה:** מאשרים פלטה וכיוון חזותי לפני ליטוש רכיבים נוספים.

---

### שלב 15 — מובייל ופעולות מהירות

**שינוי:** התאמת הפריסה הקיימת למסכים קטנים.

- מציגים כרטיס מרכזי ופריטי טיפול לפני יתר המדדים.
- מונעים כרטיס KPI בודד בשורה האחרונה.
- מגדילים אזורי לחיצה ומצמצמים טקסט משני.
- שוקלים פעולה ראשית דביקה בתחתית רק בחדר הבקרה.

**סיום השלב:** ניתן לבצע את הפעולות המרכזיות ביד אחת וברוחב 390px ללא גלילה אופקית.

**בדיקות:** 320px, 390px, 768px, RTL וסיבוב מסך.

**נקודת החלטה:** מאשרים את סדר התוכן במובייל בנפרד מהדסקטופ.

---

### שלב 16 — נגישות, ביצועים וסיום

**שינוי:** תיקונים ממוקדים בלבד.

- בודקים ניווט מקלדת, focus, שמות נגישים וניגודיות.
- מוודאים שצבע אינו הסימן היחיד לחומרה.
- בודקים טעינה, שגיאה, רענון ורשת איטית.
- מריצים type-check, lint, unit tests ו-build.
- מצלמים מחדש את ארבעת מצבי הבסיס ומשווים לשלב 0.

**סיום השלב:** כל הבדיקות עוברות, אין נסיגה ב-RTL או במובייל, והדשבורד עונה על שלוש שאלות המטרה.

**נקודת החלטה:** מאשרים את גרסת הדשבורד החדשה ומכינים רשימת המשך נפרדת שאינה חוסמת השקה.

## משימות שאינן נכנסות אוטומטית לתוכנית

הנושאים הבאים ייבחנו בנפרד ולא יורחבו בלי החלטה מפורשת:

- מערכת הרשאות חדשה.
- מודל Organization / Event מרובה טורנירים.
- מערכת צ'ק-אין מלאה.
- התראות Push, SMS או דוא"ל.
- תחזיות פיננסיות, תקציבים ודוחות חשבונאיים מתקדמים מעבר לגרפים שהוגדרו בשלבים 10–13.
- שינוי מנוע הטורנירים או חוקי ההגרלה.

## תבנית העבודה בכל שלב

לפני תחילת כל שלב נרשום:

1. הבעיה המדויקת שאנו פותרים.
2. צילום או תיאור של המצב הנוכחי.
3. השינוי הקטן ביותר שיכול לפתור אותה.
4. הקבצים וה-API שיושפעו.
5. מקרי הבדיקה וקריטריון האישור.

בסיום כל שלב יינתן סיכום קצר:

- מה השתנה.
- מה נבדק.
- מה נשאר פתוח.
- האם לאשר, לדייק או לבטל לפני המעבר לשלב הבא.
