# העלאת שינויי המשחק והמועדון

כתובת אתר השחקנים שנמסרה: https://38.247.146.17.nip.io/tournaments/

הקוד מחולק לשלושה מאגרים: `beny25585/backgammon` (משחק), `beny25585/backgammon-tournament-ui` (אתר שחקנים), `Maestroxr/tournaments` (API ואדמין). יש לעדכן את שלושתם לאותה גרסת העבודה לפני בדיקת השילוב. השינויים כוללים גם את שינויי החשבונות והמנויים שהיו מקומיים לפני רשימת התיקונים הזאת.

## הכנה והפעלה בשרת

1. גבה את בסיסי הנתונים והגדרות הסביבה. משוך את `master` בכל מאגר באמצעות `git pull --ff-only`.
2. הפעל את סביבת Python של כל Backend והתקן את קובץ התלויות שלו. בשרת הטורנירים נוסף `pywebpush==2.5.0`.
3. הרץ `python manage.py migrate` ב־`Backgammon Game/backend` וב־`backgammon-tournaments-backend/tournaments`. המיגרציות החדשות בטורנירים כוללות מנויי Push, תור מסירת התראות ותור פקודות מנהל. מיגרציות החשבונות והמנויים המקומיות עשויות להיות חדשות גם הן בשרת.
4. התקן תלויות ובנה את `Backgammon Game/frontend`, את אתר השחקנים ואת `admin-frontend` באמצעות מנהל החבילות הקיים ו־`npm run build`. פרסם את תיקיות `dist` בנתיבים הקיימים, ואז אתחל את שרתי Django/ASGI.
5. ודא ש־`ACCOUNT_FRONTEND_URL=https://38.247.146.17.nip.io/tournaments` והגדרות SMTP האמיתיות קיימות. בדיקות ההפעלה של החשבונות דורשות כתובת HTTPS ושירות דואר. הגדרות הסודות נשארות בשרת, ולא ב־Git.

## פקודות מנהל מהטורניר למשחק

- הגדר סוד אקראי חדש באורך 32 תווים לפחות, זהה ב־`GAMELINK_COMMAND_SECRET` בשרת הטורנירים וב־`GAMELINK_COMMAND_SECRETS` בשרת המשחק (האחרון מקבל רשימה מופרדת בפסיקים לצורך החלפה מדורגת). השתמש בסוד שונה מסודות Django, כרטיסי הכניסה ודיווח התוצאות; בדיקות ההפעלה אוכפות זאת.
- `GAMELINK_BACKGAMMON_URL` בשרת הטורנירים חייב להיות כתובת שרת המשחק ללא נתיב. הנתיב `/api/link/admin-command/` חייב להיות נגיש מהשרת ולהגיע ל־Django.
- השאר את Redis/Channels המשותף לשרתים ולתהליכי הרקע פעיל.
- הפעל באופן מחזורי את `python manage.py deliver_admin_game_commands` מתוך שרת הטורנירים, כדי למסור פקודות שנכשלו זמנית. ביצוע הפקודה צריך להיות בתדירות של כמה שניות.
- הפעל באופן מחזורי את `python manage.py run_tasks` מתוך שרת המשחק בתדירות של שנייה עד כמה שניות. זהו תהליך ביצוע חד־פעמי של עבודות שהגיע מועדן, ולכן יש להפעילו שוב באמצעות מנהל התהליכים הקיים. הוא משמש גם לבדיקת ניתוק ולמסירת תוצאות חוזרת.

## התראות בטלפון

1. בתוך שרת הטורנירים, צור פעם אחת מפתחות באמצעות `python manage.py generate_push_keys --subject mailto:YOUR_CONTACT_EMAIL --output .env.webpush`, עם כתובת קשר אמיתית במקום הדוגמה. הפקודה אינה מחליפה קובץ קיים.
2. טען את שלוש ההגדרות מהקובץ לתהליכי שרת הטורנירים: `WEB_PUSH_PUBLIC_KEY`, `WEB_PUSH_PRIVATE_KEY`, `WEB_PUSH_SUBJECT`. הקובץ מכיל סוד; אין להעלות אותו ל־Git. אין לחדש את המפתחות בכל הפעלה.
3. הפעל כתהליך קבוע תחת מנהל התהליכים: `python manage.py run_push_notifications --interval 5`. התהליך מזהה משחקים מוכנים מהמסד גם כשהאתר סגור. נדרש חיבור יוצא ב־HTTPS לשירותי Push של הדפדפנים.
4. ודא שהקבצים `/tournaments/push-sw.js` ו־`/tournaments/manifest.webmanifest` נגישים ב־HTTPS, מוחזרים עם סוג התוכן המתאים ואינם מוחלפים ב־HTML של ה־SPA.
5. באנדרואיד: התחבר באתר, פתח את פעמון ההתראות, לחץ על ״הפעלת התראות״ ואשר את בקשת הדפדפן.
6. באייפון: הוסף את האתר למסך הבית, פתח אותו משם ואז הפעל התראות. Web Push נתמך ביישומי מסך הבית מ־iOS 16.4 ואילך. [תיעוד Apple](https://developer.apple.com/documentation/usernotifications/sending-web-push-notifications-in-web-apps-and-browsers).
7. סגור את האתר והכן מפגש חדש לשחקן הרשום. ודא שהודעה מגיעה בשני סוגי המכשירים, שלחיצה פותחת את הטורניר המתאים ושלא מופיעה הודעה כפולה בכל סריקה. בדוק גם כיבוי התראות ויציאה מהחשבון.

בדיקות מקומיות בדקו הרשמה, הרשאות, הצפנה, תור מסירה, ניסיונות חוזרים ו־Service Worker באמצעות שליחה מדומה. הן אינן אישור שהתראה התקבלה בטלפון אמיתי; אימות זה מתבצע לאחר הפרסום.

## בדיקות קבלה באתר

- יעד 5: מהיר 2:30 דקות, רגיל 5 דקות, איטי 10 דקות; השהיה של 10 שניות לפני ניכוי בכל תור. מעבר משחקון ורענון אינם ממלאים את הבנק.
- בנק שנגמר מסיים את כל ההתמודדות. בכניעה ללא הוצאת אבנים: שתי נקודות בסיס כפול קוביית ההכפלה.
- לאחר ששני שחקנים התחברו, נתק אחד: חזרה לפני 40 שניות מונעת הפסד; אי־חזרה גוררת הפסד. שני שחקנים חסרים עוברים להכרעת מנהל ללא מנצח אוטומטי.
- עדכון ניקוד מתחת ליעד אינו מסיים מפגש; קביעת תוצאה סופית/סיום מנהל עוצרים את המשחק אצל שני השחקנים ומציגים הודעה, גם אחרי רענון.
- תוצאת הקוביות נראית לפני הודעת ״אין מהלך פנוי״; הקוביות גדולות יותר במחשב.
- עמוד ״הטורנירים שלי״ מציג גם טורנירים פעילים וגם היסטוריה; רשימת כל הטורנירים שומרת טורנירים שנרשמת אליהם ומציגה מצב הרשמה.
- חיפוש העברות לפי משתמש עובד גם בעמוד הבא; הערה ריקה נדחית לפני שינוי יתרה; טקסט בתפריט המשתמשים קריא.
- שחקן חדש נדרש לטלפון. שחקן קיים ללא טלפון יכול להתחבר ולהשלים בפרופיל, אך לא להירשם/להיכנס למשחק דרך מסלול עוקף. מספרים קיימים לא הומצאו ולא הוחלפו.
